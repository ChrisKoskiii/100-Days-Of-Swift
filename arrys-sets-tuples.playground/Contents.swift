import UIKit

// If you need a specific, fixed collection or related values where each item has a precise position, you should use a tuple
let address = (house: 555, street: "taylor Swift Avenue", city: "Nashville")

// If you need a collection of values that must be unique or you need to check whether a specific item is in there quickly, you should use a set
let set1 = Set(["aardvark", "astronaut", "azalea"])

// If you need a collection of values that can contain duplicates, or the order matters, you should use an array
let pythons = ["Eric", "Graham", "John", "Michael", "Terry", "Terry"]

// Arrays are the most common of the three

// Tuple
let tuple = (name: "Chris", age: 31, profession: "Developer", birthYear: 1990)

// Set
let set2 = (["Chris", "Michael", "Koski"])

// Array
let array = ["UIKit", "SwiftUI", "CoreData", "MapKit"]

