import UIKit

// This creates an empty dictionary
var teams = [String: String]()
// And add entries later on
teams["Paul"] = "Red"

// This is an empty array of integers
var results = [Int]()

// This is an empty set of strings
var words = Set<String>()

// This is an empty set of integers
var numbers = Set<Int>()

// Set smust be created with < >. If you wanted, you could create arrays and dictionaries the same way
var scores = Dictionary<String, Int>()
var results2 = Array<Int>()


// We would craete empty collectoins so we can add values later on, we might not know exaclty what will be in the collection until later on.
